/// puts a timestamp on a type
pub mod stamped;

/// indexes a type
pub mod indexed;

/// tagged-types are typed that have a descriptor associated to the type (a tag)
pub mod tagged;

/// Dyad is a tagged-type where the tag is a pair
pub mod dyadic;

/// a value-type is a(ny) type that carries a value
pub mod values;

