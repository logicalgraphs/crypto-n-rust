name: env
on:
  workflow_dispatch: 

jobs:
  scheduled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Add Date to GITHUB_ENV
        run: echo "LE_DATE=$(date +'%Y-%m-%d')" >> $GITHUB_ENV
        
      - name: Seeing the Contects of GITHUB_ENV
        run: cat $GITHUB_ENV

